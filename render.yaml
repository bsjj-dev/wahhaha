services:
  - type: web
    name: wahhaha-signaling
    runtime: node
    plan: free
    buildCommand: npm install --production
    startCommand: node signaling-server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
